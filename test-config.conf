# CLI Frontend Generator Configuration - Test
# This file uses INI-like format for easy configuration

# General settings
default_type=component
create_folder=true
enable_hooks=true

# Paths configuration (using local templates for testing)
templates_dir=./cli-template
output_dir=./test-output